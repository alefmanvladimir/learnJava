две функции:

1. boolean isSum(short ar[], short sum)
// если в массиве есть сумма двух чисел, сумма которого возвращает sum, 
// return true, иначе false
(за один обход, сложность O[n])

2. sort(short ar[]) // O[n], два обхода